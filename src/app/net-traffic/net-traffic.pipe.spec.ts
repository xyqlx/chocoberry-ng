import { NetTrafficPipe } from './net-traffic.pipe';

describe('NetTrafficPipe', () => {
  it('create an instance', () => {
    const pipe = new NetTrafficPipe();
    expect(pipe).toBeTruthy();
  });
});
